# Raport z Testów — TASK-XXX

## 1. Testy Automatyczne Backend (Python)
* **Status:** `PASS` / `FAIL`
* **Komenda uruchomienia:** `$env:PYTHONPATH="app_cv"; python -m unittest discover -s app_cv\tests -v`

### Wynik konsoli:
```text
[Wklej podsumowanie działania testów automatycznych]
```

---

## 2. Testy Kompilacji Frontend (Node/Vite)
* **Status:** `PASS` / `FAIL`
* **Komenda uruchomienia:** `npm --prefix app_ar run build`

### Wynik konsoli:
```text
[Wklej podsumowanie kompilacji produkcyjnej frontendu]
```

---

## 3. Testy Manualne
* [Opisz krótko, czy przetestowano manualnie w przeglądarce / z kamerą / z OBS i jaki był wynik].
